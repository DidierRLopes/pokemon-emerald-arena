@ Opt-in practice save: L+R on the unlocked field requests a native encounter.
@ No healing, party rewrite or reward injection. The real battle owns progress.
EventScript_ArenaPracticeBattle::
	dowildbattle
	releaseall
	end
